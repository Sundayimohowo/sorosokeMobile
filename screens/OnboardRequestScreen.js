import React from 'react';

// Components
import RequestPage from '../components/RequestPage';

const OnboardRequestScreen = ({navigation}) => {
  return (
    <RequestPage navigation={navigation} />
  );
};

export default OnboardRequestScreen;
